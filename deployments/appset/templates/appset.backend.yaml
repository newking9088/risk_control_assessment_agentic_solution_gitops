apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: backend-appset-{{ .Values.appName }}-{{ .Values.env }}
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: 'https://github.com/{{ .Values.org }}/{{ .Values.repo }}.git'
        revision: main
        directories:
          - path: deployments/values/backend/{{ .Values.env }}/*
  template:
    metadata:
      namespace: argocd
      name: '{{ .Values.appName }}-{{ .Values.env }}-{{`{{path.basename}}`}}'
    spec:
      project: '{{ .Values.env }}-{{ .Values.appName }}'
      source:
        repoURL: 'https://github.com/{{ .Values.org }}/{{ .Values.repo }}.git'
        targetRevision: HEAD
        path: deployments/charts/backend
        helm:
          valueFiles:
            - '../../values/{{`{{path[2]}}`}}/{{`{{path[3]}}`}}/{{`{{path.basename}}`}}/values.yaml'
          values: |
            ingress:
              domain: {{ .Values.IngressBackendHost }}
      destination:
        namespace: {{ .Values.env }}
        server: {{ .Values.server }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
