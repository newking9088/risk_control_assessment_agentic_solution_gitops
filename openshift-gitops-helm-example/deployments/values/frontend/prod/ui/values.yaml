image:
  # Nexus path for this env's frontend image. See charts/frontend/values.yaml for the
  # port- vs path-based Nexus URL forms; the host must match the pull secret's
  # docker-server exactly.
  repository: <nexus-host>/<nexus-repo>/frontend-ui
  tag: "1.0.0"

replicaCount: 3

env:
  VITE_API_BASE_URL: "https://<your-app>.apps.<cluster-domain>"
  APP_ENV: prod

route:
  enabled: true
  domain: "<your-app>.apps.<cluster-domain>"
  path: /
  tls:
    termination: edge

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70

secretStore:
  name: aws-secrets-manager
  remotePrefix: myapp/prod/

oauth2Proxy:
  enabled: true
  oidcIssuerUrl: "https://login.microsoftonline.com/CHANGE_ME_ENTRA_TENANT_ID/v2.0"
